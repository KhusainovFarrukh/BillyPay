spring:
  application:
    name: config-server
  cloud.config.server.git.uri: https://github.com/KhusainovFarrukh/billy-pay-configs

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761}/eureka/

server.port: 8888